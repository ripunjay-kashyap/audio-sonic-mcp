# ── MCP Framework ─────────────────────────────────────────────────────────────
mcp==1.27.0

# ── Audio Download ─────────────────────────────────────────────────────────────
yt-dlp==2026.3.17

# ── Audio Processing ───────────────────────────────────────────────────────────
librosa==0.11.0
soundfile==0.13.1
numpy>=2.0.0
scipy>=1.10.0

# ── Utilities ──────────────────────────────────────────────────────────────────
psutil==7.2.2       # CPU telemetry

# ── Optional ML stack: Demucs separation + CLAP vibe vector/tags (~4 GB) ──────
# Recommended:  pip install ".[clap]"   (torch, torchaudio, transformers, demucs)
# Manual (CPU): pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#               pip install demucs transformers
# Without these the pipeline falls back to HPSS + a librosa vibe vector.
